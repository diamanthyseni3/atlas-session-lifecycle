name: Review Gate

on:
  issues:
    types: [closed, reopened]

permissions:
  contents: read
  pull-requests: write
  issues: read
  statuses: write

jobs:
  gate:
    uses: anombyte93/copilot/.github/workflows/reusable-review-gate.yml@v1
