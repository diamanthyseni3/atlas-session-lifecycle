name: Release Please

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    uses: anombyte93/copilot/.github/workflows/reusable-release-please.yml@v1
