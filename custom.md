# Start Skill Customizations

> Plain English instructions that modify `/start` behavior.
> Edit this file to change what happens when `/start` runs.
> No code needed — just write what you want in natural language.

## During Init

- (Add your init-phase customizations here)
- Example: "After brainstorming, always suggest a git branching strategy"
- Example: "Skip file organization for monorepo projects"

## During Reconcile

- (Add your reconcile-phase customizations here)
- Example: "Check for uncommitted changes before assessing soul purpose"
- Example: "Always show a progress percentage estimate"

## During Settlement

- (Add your settlement-phase customizations here)
- Example: "Require at least 3 commits before allowing bounty settlement"
- Example: "Always run tests before verifying soul purpose completion"
- Example: "Skip settlement if the session was less than 10 minutes"

## Always

- Keep tone direct and concise. No fluff.
- Example: "When creating session context entries, include the git branch name"
- Example: "Never suggest closing a soul purpose unless all tests pass"
